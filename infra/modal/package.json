{
  "name": "@zapp/modal-infra",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run",
    "modal:publish": "tsx publish.ts publish",
    "modal:smoke": "tsx publish.ts smoke"
  },
  "dependencies": {
    "@zapp/sandbox-service": "workspace:*",
    "zod": "3.25.76"
  },
  "devDependencies": {
    "@types/node": "22.20.1",
    "tsx": "4.23.5",
    "vitest": "3.2.7"
  }
}
