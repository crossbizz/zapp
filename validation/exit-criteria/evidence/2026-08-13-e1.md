# E1 integrated evidence — 2026-08-13

Baseline: `da42a617d426571b88a653a02ac2ffdfddeb7dda`.

One signed-in browser session starts at the home prompt and remains in the
unified builder while it answers structured clarification cards, approves the
specification and plan, observes a real authenticated preview, redirects the
remaining work with an iteration, evaluates readiness, approves the release,
and completes deployment. The composition runs the production autonomous
Temporal workflow and production staged-deploy workflow; only external provider
and persistence adapters are local fakes.

The browser assertion binds the iteration to the dependent task, verifies the
public preview session exchange and signed credential redemption, requires the
release-approval request to precede deploy, checks all eight deployment stages,
and rejects a terminal-oriented user path. Complementary autonomous and deploy
suites cover durable replay, exact approvals, cancellation, provider-stage
ordering, and terminal outcomes.

| Command | Result |
|---|---|
| Integrated E1 Playwright journey | 1/1 passed |
| Autonomous Temporal integration | 10/10 passed |
| Staged deployment workflow | 4/4 passed |
