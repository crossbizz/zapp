export {};

const provider = '@ai-sdk/openai';
const slots = [console.log];
let alias = slots;
alias = [];
const source = { value: 0 };
alias.push({ ...source });
alias.unshift(require);
slots[0](provider);
