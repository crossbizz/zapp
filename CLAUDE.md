# zapp.build

Full agent instructions: @AGENTS.md

Quick orientation: execution plans in `docs/plans/` (start at `00-master-plan.md`), tracker in `tasks/todo.md`, spec in `docs/zapp-build-prd.md`. One task at a time per the AGENTS.md task loop; stack decisions in master plan §2 are locked.
